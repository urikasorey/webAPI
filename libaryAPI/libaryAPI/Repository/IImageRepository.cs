using libaryAPI.Models;

/*namespace libaryAPI.Repository
{
	public interface IImageRepository
	{
		ImageUpload(Image, image);
		List<Image> GetAllInfoImages();
		(byte[],string,string)DowloadImage(string Id);
	}
}*/
